{
  "_comment": "Sweep 1 — VIX entry band. Default IC enters only at VIX 16-22. Standalone_v1 ran with bypass, so the band was never actually filtering. Question: does any band setting produce a positively-validating IC on gdfl_v2? Cartesian = 4 vix_min x 4 vix_max = 16 configs (some redundant where min>=max are filtered by strategy at runtime).",
  "base_params": {
    "wing_width_strikes": 8,
    "short_call_delta": 0.15,
    "short_put_delta": -0.15,
    "stop_loss_pct": 40.0,
    "profit_target_pct": 25.0
  },
  "grid": {
    "vix_entry_min": [12.0, 14.0, 16.0, 18.0],
    "vix_entry_max": [20.0, 22.0, 25.0, 28.0]
  }
}
